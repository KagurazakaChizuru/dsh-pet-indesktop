---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: e1503192c50cbf4a9392ca071882b90f_12a3ddceb0bc11f18f50525400aeaaa3
    ReservedCode1: S4mAZST5nSYkcjv3r3biyk5mynuJTkS+sD1+bBY45gGXswnIXckUsKKZi/8eiZRhVZ1MVH/rRzHbblqjjIry1F77jVXP5IGpVFLai68uZO/WedUv3XlTFVv283bpgr01ccPBw5++wWTi7yN8S0hlJen4UaUJPqqT2JSdUDucHEqik1ycYl/K+NhEJdA=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: e1503192c50cbf4a9392ca071882b90f_12a3ddceb0bc11f18f50525400aeaaa3
    ReservedCode2: S4mAZST5nSYkcjv3r3biyk5mynuJTkS+sD1+bBY45gGXswnIXckUsKKZi/8eiZRhVZ1MVH/rRzHbblqjjIry1F77jVXP5IGpVFLai68uZO/WedUv3XlTFVv283bpgr01ccPBw5++wWTi7yN8S0hlJen4UaUJPqqT2JSdUDucHEqik1ycYl/K+NhEJdA=
---

# dsh-pet-indesktop 整点报时功能集成说明

> 基于 https://github.com/MerZlin/dsh-pet-indesktop (v4.2.0) 源码集成「整点报时」功能。
> 集成日期：2026-09-15

## 一、功能概览

在桌宠内新增「整点报时」设置页，支持：

- 总开关：启用/停用整点报时（交付版默认开启，开箱即用）
- 整点播报：每个整点（分、秒均为 0）触发，可设开始/结束时段（如 08:00–22:00）
- 自定义时刻：精确到秒（HH:MM:SS），最多 50 条，可增删
- 语音播报：edge-tts 微软在线语音（需联网），20 种中文音色，音量可调，设置页内随时「试听」
- 随机台词/歌词：报时气泡附带随机一句电影台词或歌词（各内置 30 条，可在设置页覆盖编辑，清空恢复内置）
- 触发方式：桌宠气泡展示（复用现有 show_bubble）+ 可选语音朗读

## 二、新增/修改文件

### 新增文件（pet/ 下）

| 文件 | 说明 |
|------|------|
| `pet/chime_clock.py` | 纯逻辑：配置清洗、报时点计算、文案拼装、台词库 ChimeStore 读写 |
| `pet/chime_tts.py` | edge-tts 语音合成（后台线程）+ QtMultimedia 播放，按 (voice,text) 哈希缓存 |
| `pet/chime_service.py` | GUI 线程调度服务（QTimer 1s tick），对齐 todo_reminder.py 模式 |
| `pet/settings_chime.py` | 设置页构建 + 控件收集写回（build_chime_page / apply_chime_config） |

### 修改文件

| 文件 | 改动 |
|------|------|
| `pet/config.py` | 新增 `chime` 配置键：默认值 dict、reload 白名单（两处）、_normalize_pet_settings 清洗（clean_chime_data） |
| `pet/app.py` | 懒装配 ChimeService：`_chime_wanted/_ensure_chime_service/_sync_chime_service`；初始化与设置保存同步点接入 |
| `pet/modern_settings_dialog.py` | 注册「整点报时」侧栏页；_write_config 结尾调用 apply_chime_config(host) |
| `tests/test_config_schema.py` | RELOAD_WHITELIST_SNAPSHOT 追加 "chime" |
| `requirements.txt` | 新增 `edge-tts>=6.1.10` |

## 三、配置结构（config["chime"]）

```json
{
  "enabled": true,
  "hourly_enabled": true,
  "hour_start": "08:00",
  "hour_end": "22:00",
  "custom_times": ["08:30:00"],
  "speech_enabled": true,
  "voice": "zh-CN-XiaoxiaoNeural",
  "volume": 1.0,
  "quote_enabled": true
}
```

台词库独立文件：`chime_quotes.json`（多开实例为 `chime_quotes-<instance_id>.json`），语音缓存目录：`chime_tts_cache/`。

## 四、运行与验证

```bash
pip install -r requirements.txt        # 或单独 pip install edge-tts
python -m pytest tests/test_config_schema.py -q   # 5 passed
python -m pet                          # 启动桌宠
```

设置路径：桌宠右键 → 设置 → 「整点报时」。

## 五、自测结果

- `python -m py_compile`：7 个改动文件全部通过
- `ruff check`：All checks passed
- `pytest tests/test_config_schema.py`：5 passed（默认值/白名单/快照一致性）
- edge-tts 真实合成：成功生成 28.8KB mp3（音色 zh-CN-XiaoxiaoNeural）
- 离屏 GUI 冒烟：设置页构建、自定义时刻添加、音色/音量保存、台词库落盘、语音试听缓存生成均通过

## 六、注意事项

- 语音依赖 edge-tts 在线合成，首次使用需联网；离线时自动跳过语音、仅气泡报时（日志记录异常，不影响主程序）
- 新增配置键遵循仓库三处同步规约：默认值 dict + reload 白名单 + test_config_schema 快照（均已同步）
- 多开实例台词库按 instance_id 隔离

## 六、打包与交付（2026-09-15 实测）

### 打包命令

```powershell
# 冷打包（--clean 清 PYZ 缓存，避免旧包残留导致缺依赖）
python -m PyInstaller --noconfirm --clean dsh-pet-standalone-webm-chat.spec
```

> 建议 `Start-Process` 后台执行并写日志，防止会话中断后丢失构建过程。

### spec 关键点（缺一不可）

| 配置 | 作用 |
|------|------|
| `collect_all('edge_tts')` + hiddenimports | edge-tts 在线合成依赖，漏掉则试听/报时直接无声 |
| `collect_all('aiofiles')` | edge-tts 异步底层依赖 |
| `collect_all('tzdata')` + 显式 datas | 时区数据，缺失报时时间计算错误 |
| `collect_all('PySide6.QtMultimedia')` | mp3 播放后端，漏掉播放无声 |
| `collect_all('imageio_ffmpeg')` / `certifi` / `keyring` | 原有打包项，保持不动 |
| icon / COLLECT name | 保持与原版一致 |

打包后确认 `_internal` 内含 `edge_tts`、`ffmpegmediaplugin.dll`、`tzdata`。

### 交付物

```text
D:\dsh-pet-报时整合包\dsh-pet-standalone-webm-chat\   # 双击 exe 即用
D:\dsh-pet-报时整合包\使用说明.md
```

交付前配置：`chime.enabled = true`（默认开启整点报时，08:00–22:00 晓伊音色），清空测试用 custom_times。

## 七、关键踩坑与修复（集成过程实测）

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 设置报时时间后从不报时 | 启动路径只 `_ensure_chime_service()` 构造单例，`start()` 内 QTimer 从未运行（QTimer 只在设置窗口关闭回调里启动） | 启动路径改为 `_sync_chime_service()`（ensure + apply_config + start），进程启动即按配置启停 QTimer |
| 2 | 试听/报时语音异常闪退 | 语音合成与播放异常未捕获，冒泡到 GUI 线程崩掉整个桌宠 | 试听 `_preview` 与 `_on_ready` 播放均加 try/except，异常仅记日志 |
| 3 | 打包后无声 | 旧包缺 edge_tts / aiofiles / tzdata / QtMultimedia 依赖 | spec 增加 collect_all 与 hiddenimports（见上表） |
| 4 | 改 config.json 后不触发 | 运行中进程内存 config 不随磁盘热加载（只在启动/开设置时 reload） | 改配置必须重启桌宠进程；验证时先 Stop-Process 再启动新进程 |

## 八、注意事项（完整清单）

1. **融合进程内**：报时是桌宠进程内功能（QTimer + 气泡 + QtMultimedia 播放），不是独立程序/弹窗；改造必须走开源同款源码重打包，不可旁挂独立 exe。
2. **联网依赖**：edge-tts 在线合成，断网自动降级为仅气泡报时（异常已捕获，不影响主程序）。
3. **改配置需重启**：磁盘 config.json 不热加载，修改后必须重启桌宠进程才生效；或经桌宠设置界面保存。
4. **默认开启**：交付版 `chime.enabled = true`（08:00–22:00 整点 + 晓伊音色），开箱即用。
5. **防闪退加固**：所有语音合成/播放路径 try/except；QTimer 服务必须 start，不能只构造。
6. **打包依赖收集**：spec 必须 collect_all edge_tts/aiofiles/tzdata/QtMultimedia，并核对 _internal 产物。
7. **开源协议**：基于 MIT 上游 MerZlin/dsh-pet-indesktop v4.2.0 集成，保留 LICENSE 与版权声明；本项目作为其衍生整合版发布。
